# v0.1.0 发布执行包（Codex）

## 1. 当前发布结论
- 结论：可进入 v0.1.0 发布执行阶段。
- 基准分支：`main`
- 最新全量验证日期：`2026-04-05`

## 2. 最终验证快照
- 命令：`npm run smoke:release`
- 结果：通过
- backend tests：`42 passed`
- frontend unit tests：`77 passed`
- frontend e2e tests：`7 passed`
- startup fallback smoke：通过
- desktop preflight check：通过
- docker health check：通过（`/api/health` success=true）

## 3. 本次发布核心变更（摘要）
- 启动链路稳定性：Web/Desktop 端口冲突回退、Desktop preflight 纳入 release smoke 默认流程。
- 聊天主链路加固：requestId 幂等、stream 失败恢复、stop/regenerate 一致性、typed params 校验闭环。
- Tool 能力闭环：手动 Tool 面板、会话级 Skill/MCP 选择记忆、失败可执行提示。
- Memory 闭环：Qdrant 降级不阻塞聊天，Persona/Conversation scope 边界可验证。
- Live2D 接入：状态驱动（idle/thinking/talking/error）与 companion 模式体验优化。
- P2 收口：`Qdrant upsert skipped` warning 完成降噪（冷却窗口 + suppressed 汇总 + 空异常兜底）。

## 4. 提交说明（建议模板）
### 4.1 单提交模板（如本次一次性收口）
- Commit title：`release(v0.1.0): close stability loop and finalize release package`
- Commit body：
  - 完成 Web/Desktop 启动链路稳定性收口（startup fallback + desktop preflight）
  - 完成聊天主链路与 Tool 链路加固（stream/stop/regenerate/requestId/typed params）
  - 完成 Memory 与 Live2D 闭环能力接入
  - 完成 Qdrant warning 降噪（cooldown + suppressed summary）
  - 完成 v0.1.0 发布文档与验收回填

### 4.2 分提交模板（如需拆分）
1. `feat(chat): harden stream fallback and request idempotency`
2. `feat(tooling): stabilize manual tool panel with typed params guards`
3. `feat(live2d): integrate model runtime and companion state mapping`
4. `fix(memory): throttle qdrant degrade warnings and keep sqlite fallback`
5. `docs(release): backfill M7 acceptance and final smoke evidence`

## 5. 发布执行命令（建议顺序）
1. `npm run smoke:release`
2. `git add -A`
3. `git commit -m "release(v0.1.0): close stability loop and finalize release package"`
4. `git push origin main`

## 6. 发布后观察点
- 启动链路：`local:web` / `local:desktop` 在端口占用场景是否持续自动回退。
- 聊天链路：stream 中断后是否仍保持可恢复提示与无重复发送。
- Memory 链路：Qdrant 不可用时日志频率是否降低且主链路无阻塞。

## 7. 回滚策略（最小）
- 若出现阻塞问题，优先回滚到上一已通过 `smoke:release` 的提交点。
- 回滚后立即执行：`npm run smoke:release -- --skip-e2e` 进行快速健康确认。
