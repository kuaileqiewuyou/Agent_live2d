# [Release] v0.1.0-rc1

## 发布信息
- 版本：`v0.1.0-rc1`
- 目标分支：`main`
- 发布窗口：`2026-04-03 20:00-21:00 (Asia/Shanghai)`
- 发布负责人：`@kuaileqiewuyou`
- 回滚演练时间：`2026-04-03 18:30 (Asia/Shanghai)`

## 发布范围
- 聊天主链路稳定化：stream / stop / regenerate / fallback
- requestId 幂等闭环与 dedupe 入口打通
- 手动 Tool 面板与 typed params 校验闭环
- Skill / MCP 会话级选择记忆与恢复
- Memory（SQLite + Qdrant）降级不阻塞聊天主链路
- Live2D 状态驱动与 companion 模式体验优化
- Web / Desktop 启动链路稳定化（端口回退 + desktop preflight）
- Docker 本地部署链路稳定化（app + qdrant）

## 风险评估
- Provider 行为差异仍可能带来边缘响应差异（由统一抽象层兜底）
- 本地网络、代理、系统权限差异可能影响 Desktop 启动稳定性
- Qdrant 不可用场景下 Memory 降级可用，但检索体验会下降

## 回滚方案
1. 回滚到上一已通过 `smoke:release` 的提交点
2. 执行：`docker compose down && docker compose up --build -d app qdrant`
3. 验证：`GET /api/health`、Web 页面可发送一轮消息
4. 若仍异常：保持回滚版本并暂停发布

## 发布前检查清单
- [x] `python -m pytest -q` 通过
- [x] `npm run test:unit` 通过
- [x] `npm run test:e2e` 通过
- [x] `npm run smoke:release` 通过
- [x] Docker health 检查通过（`/api/health` success=true）
- [x] CONTRACT 对齐与前后端联调一致性检查通过

## 最终验证快照（2026-04-05）
- `npm run smoke:release`：通过
  - backend tests: `42 passed`
  - frontend unit tests: `77 passed`
  - frontend e2e tests: `7 passed`
  - startup fallback smoke: `passed`
  - desktop preflight check: `passed`
  - docker health check: `passed`（`/api/health` success=true）

## M8 N6 集成回归快照（2026-04-06）
- `python -m pytest tests/test_stream_manual_tool_real_execution_api.py -q`：通过（`2 passed`）
- `python -m pytest tests/test_tool_agent_skill_real_call.py tests/test_tool_agent_mcp_real_call.py tests/test_message_provider_tool_call_api.py tests/test_catalog_api.py tests/test_mcp_probe_policy.py -q`：通过（`20 passed`）
- `npx tsc --noEmit`：通过
- `npm run build`：通过
- 回归结论：manual Skill / MCP 真实执行在 stream 中可回填 `tool_result`，失败场景不阻塞 `final_answer`

## M8 N9-N12 回归快照（2026-04-06）
- `python -m pytest tests/test_provider_tool_call_abstraction.py tests/test_message_provider_tool_call_api.py -q`：通过（`9 passed`）
- `python -m pytest tests/test_stream_manual_tool_real_execution_api.py tests/test_mcp_probe_policy.py tests/test_catalog_api.py tests/test_tool_agent_skill_real_call.py -q`：通过（`22 passed`）
- `npx tsc --noEmit`：通过
- `npm run build`：通过
- 回归结论：
  - OpenAI-compatible / Ollama 已优先走真实上游 stream，并可流式回填 `tool_calls`
  - Anthropic / Gemini 已从占位适配器升级为最小可用接入
  - Skill 已切换到统一 `SkillRuntimeEngine` 执行闭环（registry + workflow runtime）
  - MCP 检查支持瞬时失败重试恢复；MCP not-ready 在 stream 中会降级为 placeholder 且不阻塞 `final_answer`

## 里程碑状态
- M1 稳定性收口：已完成
- M2 聊天链路加固：已完成
- M3 Skill/MCP 闭环：已完成
- M4 Memory 闭环：已完成
- M5 Live2D 与桌面体验：已完成
- M6 rc2 发布收口：已完成
- M7 正式版收口：已完成
- M8 真实可调用能力收口：后端能力已完成，前端增量项待继续协同

## 已知问题
- P0：无
- P1：无
- P2：`Qdrant upsert skipped` warning 已完成降噪收口（冷却窗口 + suppressed 汇总 + 空异常兜底）

## 变更回填摘要
### 2026-04-05
- 完成 M7 收口回填（文案统一、双模式可用、README 前端联调说明）
- 完成 P2 降噪收口回填（Qdrant warning）
- 完成 v0.1.0 发版前最终全量回归（含 E2E）

### 2026-04-06
- 完成 M8 N6 集成回归补齐（fake MCP + fake Skill + stream 主链路）
- 新增 stream 失败降级集成用例，确认工具执行失败不阻塞最终回复
- 完成 M8 N9-N12 收口：
  - Anthropic/Gemini 最小可用接入
  - OpenAI-compatible/Ollama 真实 stream 优先 + tool_calls 流式回填
  - SkillRuntimeEngine 统一执行闭环
  - 失败重试与降级集成回归补齐

### 2026-04-04
- 完成 Desktop preflight 纳入 release smoke 默认流程
- 完成 startup 端口回退回归与 Desktop 受控回归
- 完成 dedupe 入口回归与 smoke 验证

### 2026-04-03
- 完成首轮发布前 smoke、Desktop 人工验收与文档回填

## 发布执行入口
- 发布执行包：`docs/releases/v0.1.0-release-execution.md`
