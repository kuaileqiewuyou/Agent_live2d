# [Release] v0.1.0-rc1 - 2026-04-03

## 发布版本

v0.1.0-rc1

## 目标分支

main

## 发布时间窗口

2026-04-03 20:00-21:00 (Asia/Shanghai)

## 发布责任人

@kuaileqiewuyou

## 计划回滚演练时间

2026-04-03 18:30 (Asia/Shanghai)

## 发布范围

- 手动 Tool typed params 前后端校验闭环
- typed params 负向 E2E（类型错误拦截发送）落地并通过
- Docker 本地部署链路收口（端口统一为 8001、健康检查补齐）
- CI 补充：
  - Desktop + Docker Smoke
  - Release Smoke
  - Release Process Guard（main 分支生效）
- 发布协作模板：
  - Release Ticket Issue Template
  - PR 手动发布检查清单模板

## 风险评估

- 聊天主链路在离线回退模式与真实 Provider 模式下仍可能存在行为差异
- Tool 参数校验前后端一致性仍需持续关注字段命名演进
- 本地 Windows 网络/代理环境差异可能影响 GitHub、Provider、MCP 可用性
- Docker 首次构建耗时较长，网络波动时可能拉镜像失败

## 回滚方案

1. 回滚到上一个稳定 tag/commit（建议 `v0.0.x` 稳定点）
2. 重新构建并启动：
   - `docker compose down`
   - `docker compose up --build -d app qdrant`
3. 验证：
   - `curl.exe -sS http://127.0.0.1:8001/api/health`
   - 打开 `http://localhost:1420` 并完成一轮会话发送
4. 若仍异常，恢复上一个稳定产物并暂停本次发布

## 发布前检查清单（必须完成）

- [x] 本地后端测试通过（`python -m pytest -q`）
- [x] 前端单测通过（`npm run test:unit`）
- [x] 前端 E2E 通过（`npm run test:e2e`）
- [x] 一键 smoke 通过（`npm run smoke:release`）
- [x] Docker 健康检查通过（`/api/health`）
- [x] 已确认本次变更无未评估的数据结构/接口破坏性修改

## 发布说明草稿

- 新增：
  - typed params 后端校验失败链路 E2E 覆盖（stream -> fallback -> 422 展示）
  - settings 文件损坏自动恢复（原子写入 + 并发锁 + corrupt 备份）
- 修复：
  - Tauri v2 plugin 配置兼容问题（dialog/fs）
  - package.json 重复 key 警告
- 兼容性说明：
  - 当前仍以本地单用户模式为主，优先保证本地可运行与桌面联调
- 已知问题：
  - 部分历史文案存在乱码遗留，不影响主链路可用性

## 发布后结果记录

发布状态：已完成本地发布前收口验证（待正式窗口执行）  
异常：无阻塞异常  
处置：
- 2026-04-03 已执行 `npm run smoke:release`，结果通过：
  - backend tests: 18 passed
  - unit tests: 33 passed
  - e2e tests: 6 passed
  - docker health: ok (`http://127.0.0.1:8001/api/health`)
- typed params 相关补充验证：
  - `python -m pytest -q tests/test_manual_tools_and_settings_api.py -k typed` 通过
  - `npm run test:e2e -- e2e/typed-params-backend-validation.spec.ts` 通过
