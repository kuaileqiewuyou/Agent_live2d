# v0.1.0-rc1 手动验收清单（Desktop / Web）

## 使用说明
- 执行人：发布负责人或指定验收人
- 执行环境：Windows 本地开发机（含 Docker Desktop、Node、Rust/Tauri）
- 结果标记：`[x]` 通过，`[ ]` 未通过（附备注）

---

## A. 预检查
- [x] `npm install` 成功
- [x] `npm run desktop:doctor` 通过（cargo / rustc / tauri cli 均可用）
- [x] Docker Desktop 可用（`docker version` 正常）

---

## B. Docker 后端链路验收
### B1. 启动与健康
- [x] `npm run local:up` 成功
- [x] `GET http://127.0.0.1:8001/api/health` 返回 `success=true`
- [x] `docker compose ps` 显示 `agent-live2d-backend`、`agent-live2d-qdrant` 为 `healthy`

### B2. 功能烟测
- [x] Web 可创建会话并发送消息
- [x] Tool Panel 可执行 Skill/MCP
- [x] typed params 合法可发，非法可拦截

---

## C. Desktop 链路验收（Tauri）
### C1. 启动
- [x] `npm run local:desktop` 可成功拉起
- [x] 无 `os error 5` / `tauri dev exited with code` 阻塞错误
- [x] 主界面可正常进入聊天页

### C2. 交互
- [x] A/B 会话草稿隔离与恢复正常
- [x] 背景透明度设置可保存并恢复
- [x] companion 模式可正常使用，聊天输入不被遮挡

### C3. 稳定性
- [x] 多次重启 Desktop 不阻塞
- [x] 端口冲突场景可自动回退

---

## D. Web 链路验收（建议）
- [x] `npm run local:web` 成功
- [x] 刷新后会话状态、工具选择、背景配置可恢复
- [x] 聊天主链路（stream / stop / regenerate / fallback）可用

---

## E. 收尾
- [x] `npm run local:down` 成功
- [x] 验收结果已回填 release ticket
- [x] 无阻塞发布问题遗留

---

## 验收记录
- 验收日期：`2026-04-05`
- 验收人：`Codex + @kuaileqiewuyou`
- 结论：`通过`
- 备注：最终全量回归通过（含 E2E、Desktop preflight、Docker health）

## 最终全量验证快照
- `npm run smoke:release`：通过
  - backend tests: `42 passed`
  - frontend unit tests: `77 passed`
  - frontend e2e tests: `7 passed`
  - startup fallback smoke: `passed`
  - desktop preflight check: `passed`
  - docker health check: `passed`（`/api/health` success=true）

## P2 收口记录
- [x] `Qdrant upsert skipped` warning 降噪已完成（冷却窗口 + suppressed 汇总 + 空异常文案兜底）
